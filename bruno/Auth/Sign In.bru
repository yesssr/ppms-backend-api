meta {
  name: Sign In
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/sign-in/email
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@lexa.com",
    "password": "admin123"
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}
